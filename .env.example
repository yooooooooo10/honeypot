# Honeypot Environment Variables
# Copy this file to .env.local for local development

# Company name used in fake content and branding
# If not set, random company names will be used
COMPANY_NAME=TechCorp Inc

# Optional: Custom domain for fake URLs in responses
# If not set, random domains will be used
COMPANY_DOMAIN=techcorp.com

# Optional: Contact email for fake admin interfaces
# If not set, random emails will be used
ADMIN_EMAIL=admin@techcorp.com

# Optional: Enable/disable detailed logging
# Set to 'true' to log all requests (including non-honeypot)
# Set to 'false' to only log honeypot triggers
VERBOSE_LOGGING=false

# Optional: Custom webhook URL for alerts
# Uncomment and set to receive notifications for honeypot triggers
# WEBHOOK_URL=https://your-webhook-service.com/alerts

# Optional: Rate limiting configuration
# Maximum requests per minute from a single IP
# MAX_REQUESTS_PER_MINUTE=60

# Optional: Response delay configuration (in milliseconds)
# Minimum and maximum delay to add to responses for realism
# MIN_RESPONSE_DELAY=100
# MAX_RESPONSE_DELAY=600

# Optional: Enable geographic blocking
# Comma-separated list of country codes to block (ISO 3166-1 alpha-2)
# BLOCKED_COUNTRIES=CN,RU,KP

# Optional: Custom server headers
# Override default random server headers with specific ones
# SERVER_HEADER=nginx/1.18.0
# POWERED_BY_HEADER=PHP/8.1.2

# Development settings
# Set to 'true' when running locally to enable debug features
# DEV_MODE=true

# Optional: Custom timezone for timestamps
# TIMEZONE=UTC

# Optional: Language/locale for responses
# LOCALE=en_US
